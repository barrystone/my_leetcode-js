/**
 * @param {string} a
 * @param {string} b
 * @return {string}
 */
const inp1 = '11';
const inp2 = '1';

var addBinary = function (a, b) {
  const minStrLen = a.length < b.length ? a.length : b.length;

  //todo
  return minStrLen;
};

console.log(addBinary(inp1, inp2));
